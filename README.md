# SamMasterProject

**@RestController** is used to build REST APIs.

Every method returns JSON/XML directly (no HTML views)

**@Controller** is used for traditional MVC web applications that return HTML views (Thymeleaf, JSP, FreeMarker, etc.)

